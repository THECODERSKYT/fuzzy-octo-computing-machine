name: Configure HAProxy with Ngrok

on:
  workflow_dispatch:

jobs:
  haproxy-ngrok:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Install HAProxy, Ngrok, and Dependencies
        run: |
          sudo apt update
          sudo apt install -y haproxy curl jq

      - name: Start Ngrok Tunnel (TCP)
        run: |
          # Start Ngrok to create a TCP tunnel on port 8080
          ngrok tcp 8080 > ngrok.log 2>&1 &
          sleep 5
          
          # Fetch the Ngrok URL (TCP format)
          NGROK_URL=$(curl -s http://localhost:4040/api/tunnels | jq -r .tunnels[0].public_url)
          
          # Check if Ngrok URL is fetched correctly
          if [ -z "$NGROK_URL" ]; then
            echo "ERROR: Ngrok URL could not be retrieved."
            cat ngrok.log
            exit 1
          fi
          
          echo "Ngrok Tunnel URL: $NGROK_URL"
          
          # Remove 'tcp://' part to use the proper format for HAProxy
          NGROK_URL=$(echo $NGROK_URL | sed 's/tcp:\/\///')
          echo $NGROK_URL > ngrok_url.txt

      - name: Debug and Verify Ngrok URL
        run: |
          NGROK_URL=$(cat ngrok_url.txt)
          echo "DEBUG: Ngrok URL is: $NGROK_URL"
          
          # Validate the Ngrok URL format (expecting '0.tcp.ngrok.io:XXXXX')
          if [[ ! "$NGROK_URL" =~ ^[a-zA-Z0-9.-]+:[0-9]+$ ]]; then
            echo "ERROR: Invalid Ngrok URL format!"
            exit 1
          fi

      - name: Backup Original HAProxy Config
        run: |
          sudo cp /etc/haproxy/haproxy.cfg /etc/haproxy/haproxy.cfg.backup

      - name: Update HAProxy Configuration
        run: |
          NGROK_URL=$(cat ngrok_url.txt)
          echo "Configuring HAProxy with Ngrok URL: $NGROK_URL"
          
          # Write the new HAProxy config file to forward to Ngrok
          sudo bash -c "echo 'frontend http-in
          bind *:8080
          default_backend http-proxy
          backend http-proxy
          server proxy1 $NGROK_URL' > /etc/haproxy/haproxy.cfg"
          
      - name: Restart HAProxy Service
        run: |
          sudo service haproxy restart

      - name: Check HAProxy Service Status
        run: |
          sudo systemctl status haproxy.service

      - name: Test Ngrok Tunnel via Curl
        run: |
          curl -x http://localhost:8080 http://google.com -L

      - name: Keep Alive
        run: |
          while sleep 60; do echo "Running..."; done
